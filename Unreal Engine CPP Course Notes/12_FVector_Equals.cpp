/*
	FVector Equals Notes

	SOURCE: https://www.udemy.com/course/unrealcourse/learn/lecture/51727163#overview

	-----------------------------------------------------------------------------------------------------------------------------------------------

	THE EQUALS FUNCTION
	- FVector.Equals
		- Used to compare to FVectors

	FVector CurrentLocation = FVector (1.f, 1.f, 1.f);
	FVector TargetLocation = FVector (1.f, 1.f, 1.f);

	bool ReachedTarget = CurrentLocation.Equals(TargetLocation);
*/